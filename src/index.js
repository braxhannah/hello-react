import React from 'react'; // from react is the library to import from
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';

ReactDOM.render(  //ReactDom is the part that knows how to write to
  <App />, //this is a Component(or an instance of
    //pretentds this is an html tag and renders.  This is a Component




//React.createElement(app);
  document.getElementById('root')
);
